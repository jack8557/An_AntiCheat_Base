package example.example.config;

import me.godead.anticheat.config.AntiCheatConfig;
import org.jetbrains.annotations.NotNull;

public class ExampleConfig extends AntiCheatConfig {

    public ExampleConfig(@NotNull String configName) {
        super(configName);
    }
}
